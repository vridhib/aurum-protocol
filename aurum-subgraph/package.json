{
  "name": "aurum-protocol-sepolia",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ aurum-protocol-sepolia",
    "create-local": "graph create --node http://localhost:8020/ aurum-protocol-sepolia",
    "remove-local": "graph remove --node http://localhost:8020/ aurum-protocol-sepolia",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 aurum-protocol-sepolia",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.98.1"
  },
  "devDependencies": {
    "@graphprotocol/graph-ts": "^0.38.2",
    "matchstick-as": "0.6.0",
    "typescript": "^5.4.5"
  }
}
